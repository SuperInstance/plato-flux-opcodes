[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-flux-opcodes"
version = "0.1.0"
description = "FLUX bytecode opcodes — instruction set, registry, execution context"
license = {text = "MIT"}
requires-python = ">=3.8"
